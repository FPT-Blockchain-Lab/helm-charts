apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lc-graph.fullname" . }}-test-connection"
  labels:
    {{- include "lc-graph.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lc-graph.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
